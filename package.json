{
  "name": "MERN-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm install && node server.js", // serves backend file with reference to react after deployment in Heroku
    "heroku-postbuild": "cd client && npm install && npm run build",  // installs front end and runs build for react scripts post deployment in Heroku
    "local": "concurrently \"cd backend && node server.js\" \"cd client && npm start\"",
    "test": "mocha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "concurrently": "^5.0.0",
    "mocha": "^6.2.2",
    "mongoose": "^5.8.0"
  }
}
